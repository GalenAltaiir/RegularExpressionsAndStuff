let br = "<br>"

// 2D Arrays

var arr = [[1,2,3,4],["one", "two", "three", "four"]]

for(i = 0; i < arr[i].length; i++){
    for(j = 0; j < arr[j].length; j++){
        document.write(arr[i, j] + br)
    }
}
